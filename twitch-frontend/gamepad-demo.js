function checkButtonInput() {
    const gamepad = navigator.getGamepads()[0];
    const downBtns = [];
    for (let i = 0; i < gamepad.buttons; i++) {

    }
}
requestAnimationFrame(checkButtonInput);